# 仪表盘时间筛选与待办交互设计

数据看板的“本周”“本月”“更多”改为真实时间筛选入口。本周按周一至周日，本月按自然月，更多通过起止日期弹窗查询；选择范围后，数据看板 KPI 与下方统计使用同一批范围数据。成员待办保留自己现有的单日日期选择，不受数据看板范围影响。

成员待办不限制显示条数，列表超过 6 条时卡片与板块随内容自然增高。待办弹窗支持键盘操作：普通输入框 Enter 保存，多行文本框 Enter 换行、Ctrl/Command+Enter 保存，Escape 取消关闭。键盘事件只挂在当前复用弹窗元素上，重复打开时覆盖旧处理器。
