# 关联目标待办每日展开设计

关联周目标或月目标的待办不复制数据库记录，而是在读取仪表盘数据时展开为每日虚拟实例。周目标按 ISO 周的周一至周日，月目标按自然月；展开起点不早于待办设置日期，终点不晚于目标周期结束。未完成任务持续出现；完成任务在完成当天显示为已完成，此前实例按进行中计算，完成次日起停止。

范围统计按每日虚拟实例计数。完成耗时从待办 `created_at` 到首次变为已完成时记录的完整完成时间戳计算。完成时间戳复用现有完成日期文本字段，避免新增数据库列；界面日期输入只读取其日期部分。
