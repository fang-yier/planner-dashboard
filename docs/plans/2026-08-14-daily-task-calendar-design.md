# 每日待办月周日嵌套设计

“每日待办”页面改为月份、周、日期三级结构。月份容纳周板块，周板块内以仪表盘成员待办相同风格展示日期卡片，每张日期卡片集中展示当天所有人的待办。日期卡片使用自适应网格，横向空间不足时自动换行，周和月容器随内容自动增高，不使用内部滚动。

关联周目标或月目标的持续待办继续使用现有虚拟每日实例，因此会进入每个实际展示日期对应的卡片。编辑或删除任意虚拟实例时仍操作原待办记录。页面保留新增、编辑、删除、状态、负责人、预估时间和完成率信息。
