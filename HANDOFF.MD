# Planner Dashboard 项目交接

> 跨设备继续工作的最新总入口请先阅读 [`PROJECT_CONTINUITY.md`](./PROJECT_CONTINUITY.md)。本文保留早期实施细节和历史记录。

> 工作偏好：每次任务完成并通过验收后，自动提交并推送到 GitHub `main` 分支。

更新时间：2026-08-18
本地项目：`C:\Users\huangxiangdong\BaiduSyncdisk\planner-dashboard`  
GitHub：`https://github.com/hxd2xff-png/planner-dashboard.git`  
线上地址：`https://mac-worker-d6gqjxsfo1cfad0e9-1409872637.tcloudbaseapp.com/mac-worker/`  
当前分支：`main`

## 当前任务状态

- 当前功能开发阶段已收尾，工作区无待提交代码。
- 最新业务改动已推送到 GitHub `main`，当前交接文档为最新状态汇总。
- GitHub 源码同步已完成；CloudBase 线上部署仍需人工确认或手动同步。
- 后续工作重点从界面微调转向数据持久化、状态历史、自动部署与自动化测试。

## 2026-08-25 数据故障切换

- Supabase 已恢复为主数据库；CloudBase 仅在 Supabase 网络超时、429 或 5xx 时临时接管。
- CloudBase 云函数使用 `planner_failover_operations` 记录有序增删改操作；Supabase 恢复后自动回放并逐条校验，校验通过后清空 CloudBase 临时表和操作日志。
- Supabase 与 CloudBase 同时不可用时，操作进入浏览器 `wb_failover_queue`，后续由同一浏览器补传。
- CloudBase 早于 Supabase 恢复时，本地队列会按原顺序转存到 CloudBase；操作使用唯一 `op_id` 去重，只有云端确认成功后才删除本地记录。
- 恢复清理只确认本轮成功回放的 `op_id`，并在数据库事务锁内判断日志是否为空；并发新操作不会被整表清理误删。
- 三张 Supabase 业务表使用 `sync_version` 乐观锁和 `last_op_id` 幂等键；旧版本写入不会覆盖新数据，而是进入 `planner_sync_conflicts`。
- 删除记录写入 `planner_sync_tombstones`；CloudBase 日志使用 `sequence_no` 保证跨用户回放顺序。
- 历史 CloudBase 数据已合并回 Supabase，校验数量为：目标 33、待办 178、在线表 15，CloudBase 对应记录均已在 Supabase 中找到。
- `planner-api` 新云函数已部署；静态 `app.js` 通过 CLI 上传时遭遇 TLS 建连中断，需在 CloudBase 静态托管中手动上传到 `/mac-worker/app.js` 后再触发首次自动清理。

## 一、已完成内容

### 1. 基础工作台

- 已实现仪表盘、月目标、周目标、每日待办、进度看板、设置、工作分工等入口。
- 支持协同模式、Supabase 数据读取与本地模式降级。
- 左侧菜单和整体视觉风格已统一。

### 2. 仪表盘

- 成员待办按成员分组，任务数量不设 6 条上限，内容增加时板块自动增高。
- 成员待办显示用户设置的预估时间，不显示实际耗时。
- 保留“今日”快捷按钮，支持前一天、后一天及指定日期。
- 成员完成率排行常驻右侧，仅按成员待办所选日期计算。
- 周目标、月目标概览采用左右轮播；仪表盘卡片只展示目标名称、备注、周期、优先级、状态和进度。
- 数据看板支持当天、本周、本月、自定义范围。
- 数据看板底部统计项保持单行，已压缩左右间距。
- 任务状态分布、成员负载分布已支持未开始、进行中、审核中、待上线、已完成、打白工。
- 趋势总览固定显示最近 7 天，不受顶部日期或数据看板周期选择影响。
- 日期联动规则：
  - 成员待办日期影响成员待办、数据看板、任务状态分布、成员负载分布。
  - 数据看板周期只影响数据看板、任务状态分布、成员负载分布。
  - 趋势总览保持独立。

### 3. 待办与目标

- 待办状态包括：未开始、进行中、审核中、待上线、已完成、打白工。
- 状态联动完成率：未开始为 0%，审核中为 90%，待上线/已完成/打白工为 100%；完成率手动调到 100% 后不再因状态变化而降低。
- 成员待办卡和右侧成员完成率排行均按每条待办的实际 `completion_rate` 求平均；审核中 90%、待上线/已完成/打白工 100% 会立即计入成员总完成率。
- 待办添加和编辑支持 Enter 确认、Esc 取消。
- 添加待办弹窗已压缩为桌面端一屏展示。
- 成员待办负责人默认使用当前登录账号名称；周目标和月目标仍保留负责人。
- 周目标/月目标关联待办规则：
  - 未关联目标：只在待办自身日期出现。
  - 关联周目标：目标周期内每天出现。
  - 关联月目标：目标周期内每天出现。
  - 未来目标关联待办也立即在成员待办中出现。
  - 完成当天保留，第二天起不再出现；周期结束后停止出现。
  - 范围统计中，持续待办每天按一次工作记录计算。
- 周目标和月目标任务卡已统一成员待办风格，并保留优先级视觉强弱；月目标支持编辑优先级。

### 4. 每日待办与进度看板

- 每日待办按“月份 → 周 → 日期”分层展示。
- 每一天是独立卡片，横向空间不足时自动换列并自适应高度。
- 支持按成员筛选每日待办。
- 进度看板按日期分组，每天顶部显示日期。
- 每天包含完整状态列并支持拖拽修改状态。
- 拖入“审核中”自动保存 90%，拖入“待上线/已完成/打白工”自动保存 100%；已有 100% 不因拖拽状态而降低。

### 5. 在线表链接

- 左侧原“工作分工”入口已替换为“在线表链接”。
- 在线表支持名称、分类、链接、用途说明、负责人和排序字段，并可新增、编辑、删除。
- 链接根据创建时选择的分类分组，每组左上角显示分类名称和数量；空分类不展示。
- 支持按名称、用途、负责人搜索以及按分类筛选，点击卡片在新窗口打开在线表。
- 数据写入 Supabase `online_sheet_links` 表，所有用户共同维护、共同可见。
- 在线表分类支持全局单板块置顶；置顶另一个板块会自动替换，所有用户看到相同排序。
- 在线表搜索使用原地卡片过滤，不重建输入框；搜索框聚焦时轮询只更新数据、不重绘页面，兼容中文输入法组合输入。
- 首次上线前需执行 `docs/supabase/online-sheet-links-migration.sql`，或复制设置页更新后的完整建表 SQL。
- 在线表迁移 SQL 可重复执行，并会修复同名旧表缺失字段；保存失败时页面会区分密钥、权限、缺表和缺字段错误。

### 6. 视觉与上线

- 仪表盘 Banner SVG 已作为中部背景展示，尺寸和透明度已调整。
- 每次改动均提交并推送 GitHub `main`。
- 最近业务提交：`f476eb1 fix: separate dashboard date scopes`。
- 上一版交接文档提交：`2d6e18d docs: add project handoff`。

## 已新增：目标板块与负责人分组

- 周目标和月目标新增板块字段，首批选项为页面、推广、活动、会员、social、直播、设计、其他。
- 每个目标保留一个可编辑负责人，新增目标默认使用当前账号真实姓名。
- 仪表盘成员待办内容和样式保持不变，仅移动到周/月目标概览上方。
- 仪表盘周/月目标按板块使用与成员待办一致的卡片式分组；已完成或周期结束日早于今天的目标不在仪表盘展示。
- 仪表盘周目标可独立选择周次，月目标可独立选择月份；两者分别持久化，不影响成员待办日期、数据看板周期和趋势总览。
- 左侧周目标/月目标管理页展示全部历史，并按板块及周次/月份分类；仪表盘选择历史周期时展示该周期全部目标，当前周期继续隐藏已完成目标。
- 周目标管理页按“板块 → 周次 → 目标卡片”展示，月目标管理页按“板块 → 月份 → 目标卡片”展示。
- 历史目标缺少板块时归入“未分配板块”。
- Supabase 目标表需要存在 `board TEXT DEFAULT ''` 字段，部署前执行设置页提供的补充 SQL。
- 在补充字段前，旧表保存会自动降级去掉 `board` 后重试；目标仍会保存到当前浏览器，但板块不会同步到云端，页面会给出提示。
- 所有用户共享云端数据前，必须以 Supabase 管理员身份执行 `docs/supabase/goal-board-access-migration.sql`；该脚本补字段、关闭相关表 RLS 并授予 anon/authenticated 读写权限。

## 二、当前问题

1. **CloudBase 未自动跟随 GitHub 部署**  
   当前可靠流程仍是推送 GitHub 后，在 CloudBase 手动同步/替换部署文件。仓库推送成功不代表线上已经更新。

2. **缺少任务状态历史表**
   当前只有任务最新状态、创建时间和完成时间。趋势总览可以统计每日新增与每日完成，但无法准确回放某条任务每天经历过的所有状态变化。

3. **代码集中在单个 `app.js`**
   功能增长后维护成本较高，状态映射、统计和页面渲染存在重复配置的风险。

4. **缺少正式自动化测试套件**
   当前主要使用 Node 静态回归脚本、语法检查和浏览器人工/自动化验收，没有固定的 `npm test` 测试项目。

5. **日期字段兼容性需要持续关注**
   历史数据可能混用 `task_date`、`date`、`due_date`、`completion_date` 和 ISO 时间。新增统计逻辑必须统一截取或转换为本地自然日。

## 三、下一步计划

按优先级建议：

1. 将工作分工周期、行列、合并信息迁移到 Supabase，实现多端同步和数据备份。
2. 新增任务状态历史/事件表，记录任务创建、状态变更、完成率变更和完成时间，完善每日趋势。
3. 配置并验证 GitHub → CloudBase CI/CD；如当前 CloudBase 套餐不支持，则保留明确的手动部署清单。
4. 为日期展开、目标关联、完成率、趋势统计建立正式单元测试。
5. 将 `app.js` 逐步拆分为数据层、日期规则、统计、页面渲染、交互事件等模块。
6. 对桌面端常用分辨率和移动端进行完整回归，重点检查单行统计、看板多列及弹窗高度。

## 四、注意事项

- 修改的实际项目目录是 `C:\Users\huangxiangdong\BaiduSyncdisk\planner-dashboard`，不要误改 `Documents\ChatGPT\planner-dashboard` 中的旧副本。
- 主要部署文件为：`index.html`、`app.js`、`style.css`、`banner-rocket.png`、`brand-logo.png`。
- 日常上线流程：
  1. 验证 JavaScript 语法和关键功能。
  2. `git add` 相关文件。
  3. `git commit -m "..."`。
  4. `git push origin main`。
  5. 在 CloudBase 手动同步/部署，并强制刷新页面确认。
- CloudBase/CDN 可能缓存约 5 分钟；必要时使用 `Ctrl+F5` 或 `Cmd+Shift+R`。
- 不要改变用户已经确认的既有状态颜色；新增状态需单独配置任务卡、看板、数据统计和成员负载映射。
- “审核中”默认联动 90%；“待上线/已完成/打白工”默认联动 100%；“未开始”默认联动 0%；已有 100% 优先保留。
- 数据看板、成员待办和趋势总览的时间范围是三套不同用途，修改时不要重新耦合。
- 关联目标的持续待办是运行时展开实例，不应重复写入数据库。
- 删除或重构日期逻辑前，必须回归未来目标、完成当天保留、周期结束停止等规则。
